# 单因子验收与评估报告字段说明

本文档用于说明 `factor_report.md` 中各字段的定义、数据来源和计算方法。

## 一、统一计算口径

### 1. 因子版本

- 原始因子值（Raw factor）：因子类按照配置参数直接计算出的值，内部后缀为 `_raw`。
- 中性化因子值（Neutralized factor）：逐交易日对原始因子值做横截面普通最小二乘回归（Ordinary Least Squares, OLS），取回归残差，内部后缀为 `_neutralization`。

中性化回归为：

$$
f_{i,t}=\alpha_t+\beta_t\ln(\mathrm{MarketCap}_{i,t})
+\sum_k\gamma_{k,t}\mathrm{IndustryDummy}_{i,k,t}+\varepsilon_{i,t}
$$

报告中的中性化因子值为 $\varepsilon_{i,t}$。行业哑变量使用申万一级行业（SW Level-1 Industry），并保留截距项、删除第一个行业类别以避免完全共线。因子值非有限、市值非正或行业缺失的股票不进入该日回归，结果保持为空。

### 2. 股票池和样本过滤

股票池为中证500历史成分股。每个交易日按照当日历史成分关系取样，并沿用数据加载代码中的 ST 和停牌过滤。报告不使用当前成分股倒推历史股票池。

### 3. 信号、买卖和调仓日期

- 信号日（Signal date）为因子截面的所属交易日 $t$。
- 可用日（Available date）为信号日后的下一个真实交易日。
- 5日收益窗口为在 $t+1$ 的复权收盘价买入，在 $t+6$ 的复权收盘价卖出。
- 未来 $h$ 日收益（Forward return）公式为：

$$
R^{(h)}_{i,t}=\frac{P_{i,t+1+h}}{P_{i,t+1}}-1
$$

- 主报告的持有期和调仓间隔均为 5 个交易日。组合收益只使用每隔 5 个交易日抽取的非重叠调仓截面，避免把重叠的5日收益重复复利。

### 4. Top、Bottom 和分组方向

Top 表示因子值最高的一侧，Bottom 表示因子值最低的一侧。该定义只由因子数值排序决定，不会因为经济方向或 IC 正负而自动互换。因此，负向有效因子通常需要同时查看 Bottom20 表现。

### 5. 区间收益、超额收益和回撤

设非重叠调仓期的组合收益为 $r_1,\ldots,r_T$：

- 区间绝对收益（Period absolute return）：

$$
R_{\mathrm{abs}}=\prod_{t=1}^{T}(1+r_t)-1
$$

- 单期相对基准收益（Relative period return），其中 $b_t$ 为同期中证500收益：

$$
r^{\mathrm{rel}}_t=\frac{1+r_t}{1+b_t}-1
$$

- 区间超额收益（Period excess return）：

$$
R_{\mathrm{excess}}=\prod_{t=1}^{T}(1+r^{\mathrm{rel}}_t)-1
$$

- 绝对最大回撤（Absolute maximum drawdown）：先计算组合净值 $NAV_t=\prod_{j=1}^{t}(1+r_j)$，再取

$$
MDD=\min_t\left(\frac{NAV_t}{\max_{s\le t}NAV_s}-1\right)
$$

最大回撤通常为非正数，例如 `-20%` 表示从历史净值峰值最多下跌20%。

## 二、基本信息

| 字段 | 定义和来源 |
|---|---|
| 因子名称 | 配置文件中的因子唯一名称。 |
| 因子类别 | 来自 `factor_categories_5947.json` 的六类唯一分类。单因子数学变换继承基础因子类别；多基础因子组合继承输入因子中全时段5日原始因子绝对IC均值最大的因子类别。 |
| 经济方向 | `+` 表示预先编码的经济逻辑为因子值越高、预期收益越高；`-` 表示因子值越高、预期收益越低；`~` 表示不提供经济方向。变换及组合因子固定为 `~`。该字段来自静态规则，不根据本次回测 IC 反推。 |
| 计算公式 | 根据因子配置、算子和公式映射确定的数学表达式。 |
| 参数 | 当前因子配置的完整参数字典。 |
| 股票池 | 本次回测使用的历史成分股指数。当前为中证500历史成分股。 |
| 因子数据区间 | 本次加载并参与因子计算的市场数据起止交易日。 |
| 5日收益有效区间 | 能形成有效5日未来收益和有效日频 IC 的信号日起止范围。尾部因缺少 $t+6$ 价格而自然提前结束。 |
| 调仓频率 | 组合重新排序并形成持仓的间隔，当前为5个交易日。 |
| 收益窗口 | 当前为 `T+1买入、T+6卖出`。 |
| 样本过滤 | 当前生效的历史成分股、ST、停牌过滤。 |
| 中性化 | 中性化版本使用的暴露项和方法，当前为对数市值、申万一级行业和 OLS 残差。 |
| 空缺率 | 原始因子值在过滤后应有的 `(date, symbol)` 样本中，非有限因子值所占比例：`空缺数 / 应有观测数`。中性化原则上继承原始缺失，并可能因市值或行业数据不可用而额外缺失。报告按要求只展示原始因子空缺率。 |

## 三、核心指标

### 1. 日频 IC

每个有效信号日，在当日股票截面上计算因子值与5日未来个股收益的 Spearman 秩相关系数（Spearman Rank Information Coefficient, Rank IC）：

$$
IC_t=\mathrm{SpearmanCorr}(f_{i,t},R^{(5)}_{i,t})
$$

单日共同有效股票少于3只时，该日 IC 记为空。

### 2. 核心字段

| 字段 | 定义和计算方法 |
|---|---|
| 因子版本 | 原始因子值或中性化因子值。 |
| IC均值 | 对完整日频 IC 序列按5日收益窗口拆成5个 offset 子序列，分别求均值，再对有效 offset 均值等权平均。 |
| IC标准差 | 分别计算5个 offset IC 子序列的样本标准差，再对有效标准差等权平均。 |
| ICIR | 信息系数比率（Information Coefficient Information Ratio）。每个 offset 先计算 `子序列IC均值 / 子序列IC标准差`，再对有效 offset 的 ICIR 等权平均。它不直接用表中已四舍五入的 IC均值除以 IC标准差。 |
| t-stat | 对完整日频 IC 序列回归常数项得到 IC 均值的 t 统计量，标准误使用 Newey-West/HAC（Heteroskedasticity and Autocorrelation Consistent）修正；5日收益的最大滞后阶数为4。 |
| p-value | 上述 HAC t 检验中“IC均值等于0”的双侧 p 值。 |
| 胜率基准符号 | 有经济方向时直接使用 `+` 或 `-`；方向为 `~` 时，分别以该因子版本全时段 IC均值的符号作为胜率基准。全时段 IC均值为0时显示 `~`，胜率不可计算。 |
| 自然周数 | 将有效日频 IC 按自然周分组后的有效周数。自然周使用 `W-FRI` 周期，当周全部有效交易日均参与周均值。 |
| 自然周IC胜率 | 先计算每周日频 IC 的算术平均值，再乘以胜率基准符号；结果严格大于0的周数占全部有效周数的比例。等于0视为未胜。 |
| IC偏度 | 完整有效日频 IC 序列的样本偏度（Sample skewness）。 |
| IC滞后1期自相关系数 | 自然周 IC 均值序列的一阶自相关系数，即相邻两个自然周 IC 的 Pearson 自相关；“1期”在此处表示1个自然周。有效周数少于3时不可计算。 |
| 最大连续失效周数 | 按胜率基准定向后，周 IC 小于或等于0的最长连续周数。 |
| 最长失效区间 | 上述最长连续失效周对应的起止自然周。没有失效周时显示“无”。 |
| 5日单边换手率 | 在每个5交易日调仓截面选取因子值最高20%的股票并等权配置。相邻调仓期单边换手率为 $\frac{1}{2}\sum_i|w_{i,t}-w_{i,t-1}|$，报告展示所有有效调仓期的算术平均值。 |
| Top20区间绝对收益 | 每个非重叠5日调仓期，因子值最高20%股票的等权平均未来收益按期复利。等同于五分组中的 Group 5 区间绝对收益。 |
| Bottom20区间绝对收益 | 每个非重叠5日调仓期，因子值最低20%股票的等权平均未来收益按期复利。等同于五分组中的 Group 1 区间绝对收益。 |
| Top20绝对最大回撤 | Top20组合绝对净值序列的最大回撤。 |
| Bottom20绝对最大回撤 | Bottom20组合绝对净值序列的最大回撤。 |

## 四、细节表现

### 1. IC衰减

IC衰减分别使用 5、10、15、20、40、60 个交易日的未来收益重复计算 IC均值、ICIR、t-stat 和 p-value。非5日持有期只计算 IC 相关统计，不重复生成不展示的组合指标。

| 字段 | 定义和计算方法 |
|---|---|
| 持有期 | 未来收益覆盖的交易日数量，同时也是 offset IC 统计的拆分数量。 |
| 原始/中性化IC均值、ICIR、p值 | 对应因子版本、对应持有期的 IC 统计。 |
| IC峰值持有期 | 在六个测试持有期中，`abs(IC均值)` 最大的持有期。 |
| 峰值IC强度 | 峰值持有期的 `abs(IC均值)`。 |
| IC半衰期（交易日） | 从峰值持有期之后开始查找：第一个 `abs(IC均值)` 小于或等于峰值强度50%，且此后所有已测试持有期均持续不高于该阈值的位置。结果为“该持有期减峰值持有期”。若截至60日仍未满足持续跌破条件，显示 `>60`。该指标基于离散持有期网格，不是连续日逐日估计。 |

### 2. 五分组表现

每个非重叠5日调仓截面，先以 `rank(method="first")` 打破相同因子值的并列，再用五分位分组（Quintile grouping）划分为 Group 1 至 Group 5。Group 1 为最低20%，Group 5 为最高20%。各组内股票等权。

| 字段 | 定义和计算方法 |
|---|---|
| 分组 | Group 1 至 Group 5，组号越大代表因子值越高。 |
| 区间绝对收益 | 该组各非重叠调仓期收益的复利累计结果。 |
| 区间超额收益 | 该组相对同期中证500的逐期相对收益复利结果。 |
| 绝对最大回撤 | 该组绝对净值的最大回撤。 |

### 3. Top/Bottom阈值表现

报告保留 Top/Bottom 10%、20%、30%。10%和30%按每个截面因子值稳定排序后分别选择 `ceil(比例 × 有效股票数)` 只股票；20%使用五分组的 Group 5 和 Group 1。组内股票等权。

字段中的区间绝对收益、区间超额收益和绝对最大回撤使用本文第一节的统一公式。

### 4. 滞后检验

滞后0、1、2、3个交易日分别使用 $f_{t}$、$f_{t-1}$、$f_{t-2}$、$f_{t-3}$ 对同一信号日未来收益进行完整5日评估。

| 字段 | 定义和计算方法 |
|---|---|
| 滞后交易日 | 因子值相对当前信号日向后移动的交易日数量。 |
| IC均值 | 该滞后版本的5日 IC均值。 |
| IC强度保留率 | `abs(该滞后IC均值) / abs(滞后0的IC均值)`。滞后0为100%；若基准为0则不可计算。 |
| Top20/Bottom20区间绝对收益 | 对应滞后因子排序形成的高20%或低20%组合区间绝对收益。 |
| Top20/Bottom20区间超额收益 | 上述组合相对同期中证500的区间超额收益。 |

## 五、市值表现

每个交易日只在当日中证500历史成分股内部对市值做百分位排名，并划分为小盘、中盘、大盘三个等数量区间。它不是全A股市值分组。

| 市值层 | 当日定义 |
|---|---|
| 中证500内部小盘组 | 市值百分位不高于 $1/3$。 |
| 中证500内部中盘组 | 市值百分位高于 $1/3$ 且不高于 $2/3$。 |
| 中证500内部大盘组 | 市值百分位高于 $2/3$。 |

每个市值层分别重新计算5日 IC均值以及 Top20/Bottom20 的区间绝对收益和区间超额收益。此处的 Top20/Bottom20 是该市值层内部的高低20%。

## 六、行业表现

行业表现按照申万一级行业分别计算：

| 字段 | 定义和计算方法 |
|---|---|
| 行业 | 信号日股票对应的申万一级行业名称。 |
| IC均值 | 该行业内逐日 Rank IC 的算术平均值。单日行业内共同有效股票少于3只时，该日行业 IC 为空。 |
| 自然周IC胜率 | 该行业日频 IC 按自然周求均值后，根据因子经济方向或全时段 IC 符号计算的周胜率。 |
| Top20/Bottom20区间绝对收益 | 每个非重叠调仓截面在该行业内部选择高20%或低20%股票，等权收益按期复利。 |
| Top20/Bottom20区间超额收益 | 上述行业组合相对同期中证500的区间超额收益。基准不是行业指数。 |

## 七、市场状态表现

市场状态来自 `zz500_lightgbm_prediction_4class_signals.csv`，状态含义为：

| 状态 | 名称 |
|---|---|
| 0 | 全面牛市 |
| 1 | 结构性牛市 |
| 2 | 非趋势 |
| 3 | 熊市 |

报告分别展示 `2014-2019` 和 `2020-2026` 两个时代，并分别展示原始因子值和中性化因子值。相同状态的日期先按状态连续性拆成片段，只保留不少于7个交易日的连续片段。未来收益在每个连续片段内部单独计算，不跨状态边界；每个片段从首日开始每隔5个交易日抽样。

| 字段 | 定义和计算方法 |
|---|---|
| 状态 | 状态编号和中文名称。 |
| 连续片段数 | 当前时代、当前状态中，长度不少于7个交易日的连续片段数量。 |
| 状态交易日 | 上述所有保留片段的交易日并集数量。 |
| IC均值、IC标准差、ICIR、t-stat、p-value | 在当前时代和状态片段样本内重新计算的5日指标。 |
| 胜率基准符号 | 经济方向明确时使用固定方向；方向为 `~` 时仍使用对应因子版本全时段 IC均值符号，不使用状态内 IC 反推。 |
| 自然周数、自然周IC胜率 | 当前状态片段形成的有效日频 IC 按自然周汇总后的周数和胜率。 |

## 八、年度表现

每个自然年在年内独立切片并重新计算5日指标。未来收益必须在同一个自然年内完整形成，不跨年使用下一年价格。因此年末收益窗口不完整的信号日自然剔除。

年度表中的 IC均值、IC标准差、ICIR、t-stat、p-value、胜率基准符号、自然周数和自然周IC胜率，定义均与核心指标一致，只是计算样本限制在对应自然年。

## 九、特殊显示值

- `~`：当前字段无法计算、不适用或缺少足够样本。
- `>60`：在已测试至60个交易日的离散持有期范围内，未观察到满足持续跌破条件的 IC 半衰期。
- `无`：没有出现连续失效周。
